#cloud-config
users:
  - name: user
    groups: sudo
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    ssh_authorized_keys:
${ssh_keys}

write_files:
  - path: /etc/ssh/sshd_config.d/99-bastion.conf
    permissions: '0644'
    content: |
      AllowAgentForwarding yes
