#cloud-config
users:
  - name: user
    groups: sudo
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    ssh_authorized_keys:
${ssh_keys}

package_update: true
packages:
  - nginx
runcmd:
  - systemctl enable nginx
